# V25母艦提案の最終引継ぎ

2026-09-22。親タスクの指示により追加造形を停止し、現時点の正本と画像を提示用として固定した。**外観は未承認であり、完成承認済みとは扱わない。**

## 提示する成果物

1. `approval-mobile.png`：上面俯瞰と機腹。新しい艦首、甲板層、整備ハッチ、肩の排熱・補強枠、4格納口を確認する。
2. `contact-sheet.png`：同一正本の7方向図。
3. `scale-comparison.png`：原寸X47を外にも配置した16:1の幅比較。小型見本を拡大していない。
4. `v24-comparison.png`：却下されたV24との比較。
5. `chunkbuster-v25.blend`、`chunkbuster-v25.glb`、`chunkbuster-v25-closed.glb`：正本と静止交換形式。

大形状は12・14・16の高さを比較して14を選択した。幅48・長さ32は維持。V24の薄い全翼から、中央船体・厚い肩装甲・切欠・低い無人センサー構造へ変更した。補給品3種は変更していない。V24の正本・画像も保存済みで、却下記録は隣のV24フォルダーにある。

## 最終検査

| 項目 | 結果・証跡 |
| --- | --- |
| 外形 | 3終端状態ともBlender `[-24,-16,0]..[24,16,14]`、正確に48×32×14 |
| 左右対称 | frame 1・40・80の反転最大誤差0。630メッシュが共通Mirrorを保持 |
| 状態遷移の外形 | 全80フレームが上記外形内。`state-envelope.json` |
| 4格納口 | 各169本、計676本の上向きレイで、有効高さ3.35未満の遮蔽0 |
| 受入高さ | 天井下面の最小値5.699999。要求有効空間Y=2.4..3.35を保持 |
| 原寸小型機 | 格納脚込みV22 4機すべてが各3×3×0.95の空間内 |
| GLB読み戻し | 開放・閉鎖とも部品別外形の最大誤差0、欠落部品0。5マーカーを照合 |
| 描画 | 7方向＋機腹＋閉鎖＋原寸比較の10描画が非空白・端切れなし |
| 元資産保持 | V24の4モデル×Blend/GLB、V22/V23参照4点の計12ファイルが既存ハッシュと一致 |
| 最終ログ | `build.log` と `verify.log` が正常終了 |

検査証跡は `validation.json`、`export-roundtrip.json`、`render-checks.json`、`state-envelope.json`、`preserved-source-checks.json`。7方向一覧・モバイル画像・原寸比較を目視した。

## モデル容量

| 項目 | 値 |
| --- | ---: |
| GLB頂点数 | 659,371 |
| 三角形数 | 769,654 |
| V22格納見本4機の三角形数 | 707,216 |
| 母艦のみの三角形数 | 62,438 |
| メッシュ / GLBプリミティブ | 630 / 636 |
| GLB材質数 | 27 |
| Blend容量 | 6,191,332 bytes |
| 主GLB容量 | 26,669,056 bytes |

設計確認用の値であり、ゲーム向け性能を承認した値ではない。承認後は見本の別インスタンス化、LOD、材質と描画回数の整理が必要。

## バックエンドへの差分

**高さだけ7から14へ変更する提案。バックエンドは今変更しない。** 機腹原点、前方-Z、照射位置 `(0,-0.1,0)`、以下4受入点と待機高さ-2はV24と同じ。

- slot 0：`(-7.15,2.4,-5.2)`
- slot 1：`(7.15,2.4,-5.2)`
- slot 2：`(-3.6,2.4,7.8)`
- slot 3：`(3.6,2.4,7.8)`

`runtime-contract.json` に差分を明記した。高さ14へのゲーム側整合とRuntimeDroneMesh変換は、明示承認後に親タスクが調整する。

## 残る課題と境界

- 676本のレイは開放格納口への垂直進入をサンプル検査したもの。全三角形の任意方向・連続移動の無干渉証明ではない。
- シャッターの終端形状と80フレーム外形は検査済みだが、途中の内部干渉・実用的な収納機構は未検証。
- 外観は承認待ち。特に後面は前面・肩側面より情報密度が低く、後部排熱形状が外板に隠れる部分が残る。追加造形停止の指示に従い、ここでは修正を広げていない。
- runtime、アイテム、バックエンド、installed資産を変更していない。ゲーム起動・インストール・commitも実施していない。
- 親タスク側のUI検査とコードテストは別作業であり、このモデル検査の実績には合算しない。
